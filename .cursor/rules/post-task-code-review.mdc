---
description: 完成有代码改动的任务后，自动做一轮本地代码审查再收尾
alwaysApply: true
---

# 任务完成后自动 Code Review

有**实质代码改动**且即将宣称任务完成时，先做一轮本地审查，再给结论。纯问答、只改文档/规则、或用户明确说「跳过 review」时除外。

## 审查范围

优先看**本次任务改动的文件**（本轮对话中新增/修改过的代码）。必要时对照相邻调用方与现有约定，不扫全仓库。

## 检查重点

1. **正确性**：逻辑是否实现了用户目标；明显 bug、空指针/空值、错误分支
2. **边界**：空输入、失败路径、并发/幂等等与改动相关的边界
3. **一致性**：是否符合本项目约定（如接口信封、鉴权、仅 GET/POST、接口设计先行等已有规则）
4. **可维护性**：是否引入无关重构、死代码、或过度抽象

## 输出与后续

用简洁列表给出发现（按严重程度：阻断 / 建议 / 可选）；无问题则一句说明「review 通过」。

- **不要擅自根据 review 结果继续改代码**；列出问题后等用户确认再修
- 若用户接着说「按 review 修」或指定某几条，再改
- 不替代表达式测试/运行验证；该跑的检查仍应跑，review 是额外一层
